# Blockchain Configuration Example
# Copy this file to .env.local and fill in your values

# Thirdweb Configuration
# Get your client ID from https://thirdweb.com/dashboard
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_thirdweb_client_id_here

# Optional: Thirdweb secret key for server-side operations
# Only needed if you're doing server-side blockchain operations
THIRDWEB_SECRET_KEY=your_thirdweb_secret_key_here

# Smart Contract Addresses
# Deploy the contracts and add the addresses here
NEXT_PUBLIC_ESCROW_FACTORY_ADDRESS=0x0000000000000000000000000000000000000000

# Network Configuration
# For production, use polygon mainnet RPC
# For development, use polygon amoy testnet RPC
NEXT_PUBLIC_POLYGON_RPC_URL=https://polygon-rpc.com
NEXT_PUBLIC_POLYGON_AMOY_RPC_URL=https://rpc-amoy.polygon.technology

# Optional: Custom RPC URLs (if you have your own RPC endpoints)
# NEXT_PUBLIC_CUSTOM_RPC_URL=https://your-custom-rpc.com

# Chain Configuration
# Set to "production" to use Polygon mainnet, anything else uses testnet
NODE_ENV=development

# Platform Wallet (for deploying contracts)
# This is the wallet that will own the factory contract
# WARNING: Never commit private keys to version control
# Use environment variables or secure key management
PLATFORM_WALLET_PRIVATE_KEY=your_platform_wallet_private_key_here

# Gas Configuration (optional - has defaults)
# MAX_PRIORITY_FEE_PER_GAS=30000000000
# MAX_FEE_PER_GAS=50000000000

# Platform Fee Configuration (optional - has defaults)
# PLATFORM_FEE_PERCENTAGE=2.5
# PLATFORM_FEE_BASIS_POINTS=250
